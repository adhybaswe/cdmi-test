/*
 * Tabs Messages
 *
 * This contains all the text for the Tabs component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Tabs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Tabs component!',
  },
});
